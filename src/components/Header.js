import React from "react";

const styles = {
  header: {
    background: "green",
  },
};

function Header() {
  return <header style={styles.header} className="Header"></header>;
}

export default Header;
